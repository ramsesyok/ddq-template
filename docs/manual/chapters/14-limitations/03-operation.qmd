## 運用上の注意

### 出力先の共有

PDF と HTML は同じ作業フォルダ（`_book`）を使うため、**あとから実行したほうが
前の出力を消す**。両方残すときは PDF → HTML の順で実行する（12章）。

### 生成物を直接編集しない

次のファイルは生成物であり、編集しても次のビルドで上書きされる。

| ファイル | 生成元 |
|----------|--------|
| `<執筆フォルダ>/design-doc.lua` ほか機構ファイル4点 | `ddq.exe` に埋め込まれた `template/` の同名ファイル |
| `<執筆フォルダ>/lib.typ`・`typst-*.typ`・`_quarto-publish.yml` | 同上 |
| `<執筆フォルダ>/_book/` | 原稿 |
| `<執筆フォルダ>/design-doc.pdf` | 原稿（中間版・発行版） |
| `<執筆フォルダ>/diagrams/mmd-*.svg` | mermaid のコードブロック |

### パスに CP932 に無い文字を使えない（Windows）

リポジトリ名・執筆フォルダ名・その上位フォルダを含め、パスには日本語を使えるが、
**Windows の ANSI コードページ（日本語環境なら CP932）に無い文字**（絵文字・波ダッシュ
`〜` U+301C・アクセント付きラテン文字など）は使えない。Windows 版 Quarto が変換フィルタへ
渡すパスをこのコードページに変換し、無い文字は `?` に潰れて復元できないためである
（Quarto 自身のファイル入出力も同じ文字で失敗する）。
`ddq init` / `ddq add` / `ddq pdf` / `ddq html` はこれを検出して止める（@sec-ascii-path）。
同じ理由で、日本語版でない Windows（CP1252 など）では日本語のフォルダ名を使えない。

### リリース一式は設計書リポジトリの外に置く

展開フォルダを設計書リポジトリの中に置いて `.gitignore` で除外する運用は採らない。
`git clean -xfd` で消える・誤ってコミットするといった事故につながるためである
（@sec-place）。リリース ZIP は展開したフォルダ（`quarto-template-<版>`）のまま、
設計書リポジトリとは別の場所に置いて使う。

### `ddq.exe` が無い端末では PDF を出せない

Quarto だけの端末では HTML までしか出せない。改ページ・横向きページ・
表の分割・紙の様式の確認は、`ddq.exe` を持つ人（発行者、または執筆者自身）が
リポジトリにコミットする**中間版 PDF**で行う（5章）。

### PDF は `quarto render` 経由で作る

`typst compile` を直接呼ぶと、コールアウトのアイコンが豆腐（□）になる。
必ずビルドスクリプトか VSCode の Quarto 拡張から出力する（12章）。

### 大量の図がある文書

mermaid 図は内容ハッシュでキャッシュされるが、初回のビルドでは図の数だけ
SVG 化が走るため時間がかかる。図の多い文書では、**キャッシュを含めて
持ち回る**運用にすると再ビルドが速い。
